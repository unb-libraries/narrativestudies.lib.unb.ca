version: '3.6'

services:
  drupal-mysql-lib-unb-ca:
    image: mysql:5.7
    restart: always
    command: mysqld --skip-name-resolve --max_allowed_packet=64M
    env_file:
      - ./env/mysql.env
    expose:
      - "3306"
    networks:
      - narrativestudies.lib.unb.ca

  drupal-redis-lib-unb-ca:
    image: redis:5-alpine
    restart: always
    expose:
      - "6379"
    networks:
      - narrativestudies.lib.unb.ca
    env_file:
      - ./env/redis.env

  drupal-solr-lib-unb-ca:
    image: unblibraries/solr-drupal:8.x-4.x
    expose:
      - "8983"
    networks:
      - narrativestudies.lib.unb.ca
    entrypoint:
      - docker-entrypoint-multiple.sh
      - narrativestudies.lib.unb.ca:/solr-conf

  mailhog:
    image: mailhog/mailhog
    expose:
      - "1025"
      - "8025"
    ports:
      - "4089:8025"
    networks:
      - narrativestudies.lib.unb.ca

  narrativestudies.lib.unb.ca:
    build:
      context: .
      args:
        - COMPOSER_DEPLOY_DEV=dev
    restart: always
    container_name: narrativestudies.lib.unb.ca
    expose:
      - "80"
    ports:
      - "3089:80"
    env_file:
      - ./env/drupal.env
    networks:
      - narrativestudies.lib.unb.ca
    volumes:
      - ./config-yml:/app/configuration
      - ./custom/modules:/app/modules/custom
      - ./custom/themes:/app/themes/custom
      - ./tests:/app/tests

networks:
  narrativestudies.lib.unb.ca:
    name: narrativestudies.lib.unb.ca
